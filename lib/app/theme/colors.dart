import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF00897B); // Teal
  static const Color secondary = Color(0xFFFF9800); // Orange/Amber
  static const Color accent = Color(0xFF4DB6AC); // Light Teal

  // Background & Surfaces
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFD32F2F);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Glassmorphism
  static const Color glassWhite = Color(0x1FFFFFFF); // 12% opacity roughly (0x1F is ~12%)
  static const Color glassBorder = Color(0x33FFFFFF); // 20% opacity
}
